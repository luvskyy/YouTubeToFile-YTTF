This folder is intentionally included for bundling FFmpeg into the final .exe.

The build script will download an FFmpeg zip and copy:
- ffmpeg.exe
- ffprobe.exe

into this folder automatically.

If the download step fails, manually place those two executables here and rerun the build:
  .\build\build.ps1
